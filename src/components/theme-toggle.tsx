// Theme toggle is temporarily disabled — only light mode is available right now.
// To re-enable, restore the dropdown trigger and connect it back to the theme context.
export function ThemeToggle() {
  return null;
}
